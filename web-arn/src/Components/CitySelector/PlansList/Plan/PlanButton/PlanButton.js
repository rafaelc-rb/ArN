import React from 'react';
import PlanButtonStyled from './PlanButton.styled';

export default function PlanButton() {
  const handleClick = (event) => {};

  return <PlanButtonStyled onClick={handleClick}>Saiba mais</PlanButtonStyled>;
}
